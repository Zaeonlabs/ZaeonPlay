/**
 * API Proxy Routes
 *
 * GET  /api/metrics                -> combined metrics from all platforms
 * POST /api/title/update           -> update stream info on selected platforms
 * GET  /api/categories/:platform   -> search categories on a platform
 * GET  /api/chat/history           -> recent chat messages from all platforms
 * GET  /api/stream/status          -> current stream status per platform
 */

// TODO: Implement API proxy routes

export {};
